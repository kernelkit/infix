pkglibexec_SCRIPTS = bootstrap error load gen-service gen-hostname \
		     gen-interfaces gen-motd gen-hardware gen-version \
		     gen-wifi-radio mstpd-wait-online wait-interface
sbin_SCRIPTS       = dagger migrate firewall
