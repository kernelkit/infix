klishconfdir    = $(KLISHCONFDIR)
klishconf_DATA  = infix.xml

if CONTAINERS
klishconf_DATA += containers.xml
endif

if SHELL
klishconf_DATA += shell.xml
endif

if SNMP
klishconf_DATA += snmp.xml
endif
